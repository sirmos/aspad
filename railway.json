{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install --include=dev && ./node_modules/.bin/tsc"
  },
  "deploy": {
    "startCommand": "node dist/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}