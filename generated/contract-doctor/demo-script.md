# Demo script for ContractDoctor (keep the video under 90 seconds)

## 0:00 to 0:10, the hook
Show the problem in one line. What is broken or missing right now
that ContractDoctor fixes?

## 0:10 to 0:30, the call
Show a real agent calling /scan on ContractDoctor.
Let the viewer see the request going out.

## 0:30 to 0:60, the result
Show the response coming back. Point at the part that matters.
A risk score 0-100, list of vulnerability flags, a SAFE/CAUTION/REJECT recommendation, and a plain English summary.

## 0:60 to 0:80, why it matters
One line on who this helps and why they would pay 0.1 USDT for it.

## 0:80 to 0:90, close
Say the name again and where to find it. Tag #OKXAI.

## X post draft
ContractDoctor. Pre-deployment risk scanner for autonomous agents on EVM chains.

An agent about to sign or deploy a transaction calls ContractDoctor first. It scans the contract address or bytecode for common vulnerability patterns, checks verification status on-chain, and returns a risk score plus plain-language flags the calling agent can act on before greenlighting the transaction. Prevents autonomous agents from losing funds to malicious or vulnerable contracts.

Live on OKX.AI now. #OKXAI

